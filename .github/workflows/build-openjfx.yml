name: Build OpenJFX

on:
  workflow_dispatch

jobs:
  build:
    runs-on: macos-14

    steps:
      - name: Set up JDK 24
        uses: actions/setup-java@v4
        with:
          java-version: '24'
          distribution: 'temurin'

